/**
 * SingleLoading component
 *
 * A simple React component that renders a loading indicator.
 *
 * @example
 * ```
 * import React from 'react';
 * import ReactDOM from 'react-dom';
 * import SingleLoading from './SingleLoading';
 *
 * ReactDOM.render(
 *   <React.StrictMode>
 *     <SingleLoading />
 *   </React.StrictMode>,
 *   document.getElementById('root')
 * );
 * ```
 *
 * @returns {React.ReactElement} A React element representing the loading indicator.
 */
import "./SingleLoading.scss";

import React, { Fragment } from "react";
/**
 * SingleLoading component
 *
 * @function
 * @returns {React.ReactElement} A React element representing the loading indicator.
 */
const SingleLoading = () => {
  return (
    <Fragment>
      <div className="container">asdfg</div>
    </Fragment>
  );
};

export default SingleLoading;
